const initialState = {}

const newPasswordReducer = (state: any = initialState, action: any) => {

    switch (action.type) {

        default:
            return state;
    }
}





export default newPasswordReducer;
