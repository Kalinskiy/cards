const initialState = {}

const forgotPasswordReducer = (state: any = initialState, action: any) => {

    switch (action.type) {

        default:
            return state;
    }
}





export default forgotPasswordReducer;
