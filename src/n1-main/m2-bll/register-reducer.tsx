const initialState = {}

const registerReducer = (state: any = initialState, action: any) => {

    switch (action.type) {

        default:
            return state;
    }
}





export default registerReducer;
