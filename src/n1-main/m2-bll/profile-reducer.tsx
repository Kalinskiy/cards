const initialState = {
    isLogged:true
}

const profileReducer = (state: any = initialState, action: any) => {

    switch (action.type) {

        default:
            return state;
    }
}





export default profileReducer;
